export type SiteConfig = typeof siteConfig;
import  config from "../config";

export const siteConfig = {
  name: config.nickname + " links",
  description: "Transformando vidas no transito de forma positiva.",
};
