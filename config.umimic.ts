import config from "./config";

export const UmimicConfig = {
  // apiBaseUrl: "http://localhost:2000",
  apiBaseUrl: "https://dip-production-6c90.up.railway.app",
  greeting: `Olá! Nós somos a ${config.nickname}, como posso ajudar você hoje?`
};
