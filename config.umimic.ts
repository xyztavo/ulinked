import config from "./config";

export const UmimicConfig = {
  // apiBaseUrl: "http://localhost:2000",
  apiBaseUrl: "https://umimic-production.up.railway.app",
  greeting: `Olá! Nós somos a ${config.nickname}, como posse ajudar você hoje?`
};
